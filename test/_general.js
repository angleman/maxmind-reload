var should = require('should');

describe('maxmind-reload', function() {
    describe('should', function() {
    	var maxmind_reload;
    	it('load', function() {
	        maxmind_reload = require('../index.js');
        });
    });
});